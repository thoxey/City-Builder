# CarProxy is defined in res://plugins/traffic/car_proxy.gd
# Godot class_name registration makes it globally available — no import needed.
# To physically relocate this file, use the Godot editor FileSystem dock
# (right-click → Move) so UIDs and cache are updated automatically.
